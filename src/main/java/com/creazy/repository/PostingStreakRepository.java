package com.creazy.repository;

import com.creazy.domain.entity.PostingStreak;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PostingStreakRepository extends JpaRepository<PostingStreak, Long> {
}
